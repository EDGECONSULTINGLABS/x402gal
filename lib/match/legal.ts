/**
 * The one line that sits on every map screen and on the published methodology.
 * Referenced, never retyped. Wording agreed 2026-09-04 (review of the recorded run-through).
 */
export const LEGAL_LINE = "Estimates only. Nothing here is a measurement, an offer, or a water right.";

/** Where the result screen's edition + hash line resolves. Public; not behind the preview gate. */
export const METHODOLOGY_PATH = "/match/methodology";
